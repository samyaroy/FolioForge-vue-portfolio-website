/**
 * Deep links into the Teaching page.
 *
 * A semester has no id of its own, so its name is the identity — the same way
 * a project is named by its title. A description elsewhere on the site can
 * therefore point at one cohort rather than at the page as a whole.
 */
export const TEACHING_PROJECTS_TAB = 'projects'
export const SEMESTER_ANCHOR_PARAM = 'semester'
